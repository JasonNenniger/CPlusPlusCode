This extremely simple code takes a number that is inputed by the user and squares it.  It then outputs the resulting 
number through the console and to a file called "square.txt", which is included.  The example number used in 
this case was 22.
