This code takes the mass (m), radius of a sphere (r1), radius of the hollow inner sphere (r2) from user input via the 
console and calculates the moment of inertia of the sphere.  The moment of inertia is then outputted through the console.
